@using System.Web
@using System.Text.RegularExpressions;

@((MarkupString)Regex.Replace(HttpUtility.HtmlEncode(@InputValue ?? String.Empty), "\r?\n|\r", "<br />"))

@code {
	[Parameter]
	public string InputValue { get; set; } = string.Empty;
}
